# Markdown 格式规范说明文档

> **⚠️ 重要声明**
> 
> 本文档描述的是**个性化定制的 Markdown 格式**，并非标准 Markdown 语法！
> 
> 请严格按照本文档的格式要求进行修改，**不要**使用标准 Markdown 的格式规则！

---

## 一、核心格式结构

### 1.1 基本块结构

每个知识点/操作步骤使用以下格式：

```markdown
> **（序号）标题**
> 
> 描述内容（可选，如果标题已经足够清晰可省略）
> 
> *==🦜==*
> 
> ```bash
> 命令代码
> ```
```

### 1.2 格式要点

1. **引用块标记**：整个内容块使用 `>` 引用符号包裹
2. **标题格式**：`**（序号）标题**` - 加粗 + 中文括号 + 阿拉伯数字
3. **空行**：标题后必须空一行，描述后必须空一行
4. **特殊标记**：`*==🦜==*` 作为代码块前的标识符
5. **代码块**：使用标准的三个反引号包裹，指定语言类型（如 `bash`）

---

## 二、具体示例

### 2.1 标准格式示例

```markdown
> **（1）提交到暂存区**
> 
> *==🦜==*
> 
> ```bash
> git add .
> ```
```

### 2.2 带描述的格式示例

```markdown
> **（2）查看文件状态**
> 
> 查看在你上次提交之后是否有对文件进行再次修改
> 
> *==🦜==*
> 
> ```bash
> git status
> ```
```

### 2.3 多个代码块的格式

```markdown
> **（6）切换分支**
> 
> *==🦜==*
> 
> ```bash
> git checkout <分支名>
> ```
> 
> 如果分支不存在，使用 `-b` 选项来创建：
> 
> *==🦜==*
> 
> ```bash
> git checkout -b <分支名>
> ```
```

### 2.4 复杂步骤的格式

```markdown
> **（5）解决冲突步骤**
> 
> **a. 沟通优先**：与另一位开发者沟通，商定代码取舍方案
> 
> **b. 打开冲突文件**：Git 会在文件中标记冲突区域
> 
> ```
> <<<<<<< HEAD
> 你的代码
> =======
> 别人的代码
> >>>>>>> feature-branch
> ```
> 
> **c. 手动编辑**：删除冲突标记，保留需要的代码
> 
> **d. 标记为已解决**：将修改后的文件添加到暂存区
> 
> *==🦜==*
> 
> ```bash
> git add file.txt
> ```
```

### 2.5 总结块格式

```markdown
> **核心总结**
> 
> 流程是 clone -> 修改 -> add -> commit -> push；解决冲突的关键在于先沟通，再修改
```

---

## 三、编号规则

### 3.1 模块内编号

- 同一个模块（章节）内的序号从 **（1）** 开始连续编号
- 使用中文括号 `（）` 而非英文括号 `()`
- 序号为阿拉伯数字

### 3.2 新模块重新编号

当遇到新的 `##` 二级标题（新模块）时，序号重新从 **（1）** 开始

示例：
```markdown
## Scenario 1
> **（1）克隆仓库**
> **（2）创建新分支**

## Scenario 2
> **（1）冲突产生原因**  ← 重新从 1 开始
> **（2）合并前先获取**
```

### 3.3 子模块编号

如果有子步骤，使用 **a.** **b.** **c.** 等字母编号

---

## 四、标题要求

### 4.1 标题原则

1. **简洁明了**：标题应该是总结性的描述
2. **居中显示**：在实际渲染时，加粗的标题会居中显示
3. **长度控制**：如果标题过长，需要精炼为简短标题，详细描述放在下一行

### 4.2 标题优化示例

❌ **不好的标题**：
```markdown
> **（2）查看在你上次提交之后是否有对文件进行再次修改**
```

✅ **好的标题**：
```markdown
> **（2）查看文件状态**
> 
> 查看在你上次提交之后是否有对文件进行再次修改
```

---

## 五、需要移除的内容

### 5.1 移除标准 Markdown 标题

将标准的 `###` 标题移除，改为引用块格式

❌ **移除前**：
```markdown
### 查看在你上次提交之后是否有对文件进行再次修改？

> **（2）查看文件状态**
```

✅ **移除后**：
```markdown
> **（2）查看文件状态**
> 
> 查看在你上次提交之后是否有对文件进行再次修改
```

### 5.2 移除多余的空行

引用块之间只保留一个空行

---

## 六、特殊元素

### 6.1 鹦鹉标记

`*==🦜==*` 是特殊的标识符，用于标记代码块的开始

- 必须独立成行
- 前后各有一个空行
- 使用斜体和等号包裹

### 6.2 代码注释

代码块内可以包含注释，使用 `#` 符号

```markdown
> *==🦜==*
> 
> ```bash
> git add filename
> # 添加工作区所有修改的文件
> git add .
> ```
```

### 6.3 输出示例

可以在代码块中展示命令的输出示例

```markdown
> *==🦜==*
> 
> ```bash
> git status
> 
> # 输出会显示：
> # Unmerged paths:
> #   both modified:   file.txt
> ```
```

### 6.4 单行高亮 ⭐

**重要提示**：单行高亮在自定义渲染中特别好看，应该多用！

使用 `==文本==` 来标记需要高亮的内容

**使用场景**：
- 模块标题/副标题
- 重要的关键词
- 需要强调的概念

**示例 1：模块标题高亮**
```markdown
## **Scenario 1**

==模拟初入公司的git使用==
```

**示例 2：关键词高亮**
```markdown
> **核心总结**
> 
> 解决冲突的关键在于==先沟通==，再修改，最后重新提交
```

**示例 3：流程高亮**
```markdown
> **核心流程**
> 
> ==未跟踪 → `add` → 已跟踪/暂存 → `commit` → 已提交==
```

**示例 4：多处高亮**
```markdown
## Scenario 2

==解决代码冲突==

> **（8）预防冲突的最佳实践**
> 
> - 频繁拉取远程代码：==保持同步==
> - 小步提交：==避免一次性修改大量文件==
> - 及时推送：==不要长时间不推送本地代码==
```

**注意事项**：
- 不要过度使用，保持重点突出
- 适合用于标题、关键流程、重要提示
- 可以与代码符号 `` ` `` 组合使用

---

## 七、完整转换示例

### 7.1 转换前（标准格式）

```markdown
## 克隆仓库

参与一个已有项目，首先克隆远程仓库到本地：

*==🦜==*

```bash
git clone https://github.com/username/repo.git
cd repo
```

## 创建新分支

通常创建一个新分支，避免直接在master 分支开发：

*==🦜==*

```bash
git checkout -b new-feature
```
```

### 7.2 转换后（目标格式）

```markdown
> **（1）克隆仓库**
> 
> 参与一个已有项目，首先克隆远程仓库到本地
> 
> *==🦜==*
> 
> ```bash
> git clone https://github.com/username/repo.git
> cd repo
> ```

> **（2）创建新分支**
> 
> 通常创建一个新分支，避免直接在 master 分支开发
> 
> *==🦜==*
> 
> ```bash
> git checkout -b new-feature
> ```
```

---

## 八、常见错误

### 8.1 ❌ 使用英文括号

```markdown
> **(1)提交到暂存区**  ← 错误
```

### 8.2 ❌ 缺少空行

```markdown
> **（1）提交到暂存区**
> *==🦜==*  ← 错误，缺少空行
```

### 8.3 ❌ 标题后有冒号或句号

```markdown
> **（1）提交到暂存区：**  ← 错误
> **（2）查看文件状态。**  ← 错误
```

### 8.4 ❌ 引用块标记不完整

```markdown
> **（1）提交到暂存区**
> 
*==🦜==*  ← 错误，缺少 > 符号

```bash
git add .
```  ← 错误，缺少 > 符号
```

### 8.5 ❌ 保留了 ### 标题

```markdown
### 提交到暂存区  ← 错误，应该移除

> **（1）提交到暂存区**
```

---

## 九、使用指南

### 9.1 给 AI 的提示词模板

```
请按照《格式规范说明.md》中的格式要求，将以下内容转换为规范格式：

[粘贴需要转换的内容]

注意：
1. 这是个性化定制的 Markdown 格式，不是标准 Markdown
2. 严格遵循文档中的格式规则
3. 每个模块的序号从（1）开始
4. 移除所有 ### 标题
5. 确保引用块标记完整
6. 多使用 ==文本== 进行单行高亮，特别是标题、关键词、流程等重要内容
```

### 9.2 分步操作流程

1. **复制原始内容**
2. **标记模块边界**（如果有多个模块）
3. **提供给 AI**，附带格式规范文档
4. **检查输出**：
   - 序号是否正确
   - 引用块是否完整
   - 空行是否规范
   - 鹦鹉标记是否存在

---

## 十、快速检查清单

转换完成后，请检查以下项目：

- [ ] 所有内容都在引用块 `>` 内
- [ ] 标题使用 `**（序号）标题**` 格式
- [ ] 使用中文括号 `（）`
- [ ] 标题后有空行
- [ ] 描述后有空行（如果有描述）
- [ ] 包含 `*==🦜==*` 标记
- [ ] 代码块使用三个反引号
- [ ] 移除了所有 `###` 标题
- [ ] 每个模块序号从 1 开始
- [ ] 没有多余的空行
- [ ] **使用了 `==文本==` 进行单行高亮（重要内容）**

---

## 附录：完整模板

```markdown
> **（1）操作标题**
> 
> 操作描述（可选）
> 
> *==🦜==*
> 
> ```bash
> 命令代码
> ```

> **（2）操作标题**
> 
> *==🦜==*
> 
> ```bash
> 命令代码
> ```
```

---

**文档版本**：v1.0  
**最后更新**：2026-02-04
